import assert from 'assert'

describe('getRecentTimes', () => {
  it ('一番近い時刻を取得できる', () => {
    assert.equal(1, 1)
  })

  it ('指定した数だけ時刻を取得できる', () => {
    assert.equal(1, 1)
  })
  it("指定した時刻で初期化できる", function(){
    assert.equal(12, 12);
  });
})
